name: Deploy to GitHub Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Create public directory
        run: |
          mkdir -p public
          cp install.sh public/index.sh
          cat > public/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
              <meta charset="UTF-8">
              <title>Zviel's Installer</title>
              
              <!-- Open Graph / Facebook -->
              <meta property="og:type" content="website">
              <meta property="og:url" content="https://getlinux.zviel.com/">
              <meta property="og:title" content="🚀 Zviel's Linux Setup">
              <meta property="og:description" content="Deploy your full Linux environment with one command. Fully automated Arch Linux / Hyprland configuration.">

              <!-- Twitter -->
              <meta property="twitter:card" content="summary">
              <meta property="twitter:url" content="https://getlinux.zviel.com/">
              <meta property="twitter:title" content="🚀 Zviel's Linux Setup">
              <meta property="twitter:description" content="Deploy your full Linux environment with one command. Fully automated Arch Linux / Hyprland configuration.">
              
              <meta name="theme-color" content="#243b55">

              <style>
                  body { 
                      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
                      background: linear-gradient(135deg, #243b55 0%, #141e30 100%);
                      min-height: 100vh;
                      display: flex;
                      align-items: center;
                      justify-content: center;
                      margin: 0;
                      padding: 20px;
                  }
                  .container { 
                      background: white;
                      border-radius: 10px;
                      padding: 40px;
                      box-shadow: 0 10px 40px rgba(0,0,0,0.5);
                      max-width: 500px;
                      text-align: center;
                  }
                  h1 { color: #333; margin: 0 0 20px 0; }
                  p { color: #666; line-height: 1.6; }
                  code { 
                      background-color: #f6f8fa; 
                      padding: 10px;
                      border-radius: 5px;
                      display: block;
                      margin: 20px 0;
                      font-family: monospace;
                      color: #243b55;
                      word-break: break-all;
                      border: 1px solid #ddd;
                  }
                  .button {
                      display: inline-block;
                      padding: 12px 30px;
                      background: #243b55;
                      color: white;
                      text-decoration: none;
                      border-radius: 5px;
                      margin-top: 20px;
                      transition: background 0.3s;
                  }
                  .button:hover { background: #141e30; }
              </style>
          </head>
          <body>
              <div class="container">
                  <h1>🚀 Zviel's Linux Setup</h1>
                  <p>Deploy your full Linux environment with one command:</p>
                  <code>curl -sL https://getlinux.zviel.com/index.sh | bash</code>
                  <p><a href="/index.sh" class="button">📥 Download Script</a></p>
              </div>
          </body>
          </html>
          EOF
          echo "getlinux.zviel.com" > public/CNAME
          touch public/.nojekyll
      
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'public'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
